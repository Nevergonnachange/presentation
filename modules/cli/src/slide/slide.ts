import { Argv } from 'yargs';
import { ISlideArgs } from '../slide/slide-interface';

export function slideBuilder(args: Argv): Argv {
    return args;
}

export function slideHandler(args: ISlideArgs): void {

}
